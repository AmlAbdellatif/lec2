mkdir lec3
git init
touch amlabdellatif
git add .
git commit -m "LAB2"
git remote add origin git@github.com:AmlAbdellatif/lec2.git
git branch -M main
git push origin main
git branch test
git brach dev
git checkout dev
touch file
touch file1
git push origin dev
git push origin test
git checkout main
git merge dev
to romve bracn 
1- ermotly 
   git push origin :test
   2-licaly
   git branch -d test
    
    
 to make annoted tag
 git tag -a  v1.0 -m "my version"
 git push origin v1.0
 git tag
 
  to delete tag:
  1-remotely
  git push origin :refs/tage/v1.0
  2-localy
  git tag -d v1.0
